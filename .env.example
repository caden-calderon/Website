# Optional: local Python BG removal service for Linux / high-quality matte models
BG_REMOVAL_SERVICE_URL=http://127.0.0.1:9000
# Optional: override for `pnpm dev:full`
# BG_REMOVAL_SERVICE_COMMAND=ai-env; exec uvicorn python.bg_remove_service.app:app --host {host} --port {port}

# Optional: Hugging Face token for gated server-side models such as BRIA RMBG 2.0
HF_TOKEN=
